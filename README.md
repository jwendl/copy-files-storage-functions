# An Example to Copy Files from One Storage to Another using Azure Functions

This is just an example. It uses managed identity for both Azure Storage accounts.

Essentially, download, configure and hit F5.
